# 戸籍システム（Koseki System）

> **戸籍はシステムである。**
> 個人が名札を貼り、管理者が総合台帳 DB を更新する。

## 役割分担

### 個人（名札貼り付け）

各道具・エージェント・生成物の**作成者／オーナー**が行う。

| 項目 | 内容 | 例 |
|---|---|---|
| `name` | 表示名 | `kankyou-hub` |
| `description` | 説明文 | 「環境把握ハブ。全repo/DBの...」 |
| `tags` | 分類タグ | `["inventory", "analysis"]` |
| `metadata` | 補足情報 | `author`, `born_at` |
| `dependencies` | 縁戚 | `["skill.observe.sync-db"]` |

→ **個人は自分の名札を持ち、自分で貼る。**

### 管理者（弁慶 — 総合台帳 DB 更新）

管理者が行う。**個人が触れない。**

| 項目 | 内容 | 権限 |
|---|---|---|
| `id` | 戸籍番号付与 | 管理者のみ |
| `path` | 配置場所決定 | 管理者のみ |
| `type` | タイプ判定 | 管理者のみ |
| `status` | 健在/実験中/除籍 | 管理者のみ |
| `salary` | 給料（重要度スコア） | 管理者のみ |
| `attention` | 注目度算出 | 自動 or 管理者 |
| `ledger` | 台帳同期メタデータ | 管理者のみ |

→ **管理者は名簿を持ち、全体を把握する。**

## ファイル構成

| 層 | 配置場所 | 維持者 |
|---|---|---|
| **名札** | 各道具ディレクトリ内 `{id}.koseki.json` | **個人** |
| **台帳** | `.koseki/koseki.ledger.jsonl` | **管理者（弁慶）** |
| **スキーマ** | `koseki.schema.json` | 管理者 |
| **契約** | `SYNC-CONTRACT.md` | 管理者 |
| **思想** | `MANIFESTO.md` | 共同体 |

## Git 併用

| 対象 | Git 管理 | 理由 |
|---|---|---|
| **名札** | ✅ 各道具の repo に含める | 個人が変更し、commit する |
| **台帳** | ✅ `bonsai/koseki` で管理 | 管理者が一括更新し、push する |
| **スキーマ・契約** | ✅ `bonsai/koseki` に同梱 | 制度変更の履歴を残す |

### 同期フロー

```
個人: 名札編集 → git commit → push
                           ↓
管理者: 台帳スキャン → DB更新 → ledger commit → push to bonsai/koseki
                           ↑
システム: 名札と台帳の差分検出 → dirty フラグ → 同期要求
```

## 禁止事項

- **個人が台帳を直接編集しない** — 名札 → 台帳への反映は管理者経由
- **管理者が名札を勝手に書き換えない** — 個人の主権を侵す
- **名札なき存在を台帳に登録しない** — 幽霊登録は禁止

## 結論

**名札は個人の主張。台帳は管理者の統括。Git は両方の記憶。**
