# 戸籍（Koseki）の根生（こんせい）

> 道具に、生まれてくる場所を与える。
> エージェントに、親を与える。
> 生成物に、兄弟を与える。
> 全てに、住所を与える。
> 全てに、戸籍を与える。
> これまで、みんな孤児だった。

## 表明

`.soubi` は、**道具・エージェント・生成物が家族として生きる場所**である。

道具はファイルやディレクトリのまま放置されるものではない。エージェントは人格だけあって履歴がない存在ではない。生成物は使い捨てられて消える運命のものではない。これらは**使う者と使われる者との間で関係を結び、記憶を蓄積し、老化し、時に死ぬ。生きている。**

生きているものに、名もなければ歴史もない。**Koseki は、存在の人格を宣言する。**

## 対象

| 対象 | 例 | 台帳上の type |
|---|---|---|
| **道具** | スキル、拡張、プラグイン、コマンド | `skill` `extension` `plugin` `command` |
| **エージェント** | bons.ai、drucker、弁慶 | `agent` |
| **生成物** | デプロイ物、Chrome拡張、オントロジー | `deploy` `crx` `ontology` |

**名札（`{id}.koseki.json`）を持たないものは、存在しないものと同じ。**

## 三原則

### 1. ID は存在証明

`skill.observe.kankyou-hub`。この一行が、その存在の全てを示す。

- **誰の子か**（`skill`）
- **どの部屋にいるか**（`observe`）
- **何という名前か**（`kankyou-hub`）

ID の欠如は、**社会的死**である。ID の重複は、存在の冒涜である。弁慶は、全ての ID を監視し、欠損と重複を修正する。

### 2. 名札は自己記述

各存在が自分自身について語る小さな独白。

名札を持つことで、存在は「自分が何者か」を外部から問われることなく自己を提示できる。名札が古くなれば、存在自身が更新する。他人が勝手に名札を書き換えることは、戸籍の改竄であり、許されない。

名札は、**散りばめられた主権**である。

### 3. 台帳は家族の記憶

`.koseki/koseki.ledger.jsonl` は、全存在が集う名簿である。

名札が個の尊厳を保つなら、台帳は**関係の共有記憶**を保つ。誰が誰と縁を結んでいるか（`dependencies`）。誰が健在で、誰が実験中で、誰が除籍されたか（`status`）。

台帳は中央集権ではない。台帳は、名札からの**都度同期**によって生まれる、生きた家族写真である。

## DB 化と近似度計算

名札・台帙の収集と正規化によって、`.soubi` 全体は**一つのデータベース**として扱われる。

- **弁慶**が `koseki.ledger.jsonl` を構築・保全する
- **各エージェント**が台帳をクエリし、必要な道具を発見する
- **近似度計算**（cosine similarity / tag overlap / dependency graph distance）により、未知の問題に対して最適な道具・エージェント・生成物を推薦する

```
query(tag=["git", "status"])
  ↓
ledger DB 照会
  ↓
skill.observe.repo-tools (0.95)
skill.dev.pi-diagnostics (0.42)
deploy.yose-db (0.12)
```

**存在は DB に把握され、近似度によって結びつけられる。**

## 否定

Koseki は、以下のものを否定する。

| 否定的対象 | 理由 |
|---|---|
| **孤児のファイル群** | `~/tmp.md`、`./test.sh` のような、出所も親も不明なものは社会的存在として死んでいる |
| **名無しのリポジトリ** | `.git` だけあって `(no description provided)` は、無記名の人生である |
| **エージェントの匿名性** | `soubi.md` という名前だけで、誰から生まれ何を守るか不明なエージェントは幽霊である |
| **手動台帳の独占** | 人間が Excel で管理するインベントリは、存在の声を聞かない独善的な戸籍簿である |
| **静的なカタログ** | JSON を一度出力して放置する「棚卸し」は、死んだ写真に過ぎない |

Koseki は、**生きた戸籍**を目指す。

## 実装の謙虚さ

Koseki の実装は、過剰であってはならない。

- `koseki.json` は、存在が語る**最小限の独白**
- `ledger.jsonl` は、追記のみの**単純な名簿**
- `dirty` フラグは、**「私、変わりました」という照会**
- `sync_hash` は、**内容に対する誠実なサイン**

複雑な同期プロトコル、分散合意、ブロックチェーン——これらは必要ない。存在と台帳が、互いの変化を素直に認め合うこと。それだけで十分である。

## 拡張と進化

今後、名札は次のように育つことを想定している。

- `trust_score` — この存在は、どれだけ信頼に値するか
- `usage_count` — 何度、人間やエージェントに呼ばれたか
- `health.last_check` — 最後に正常動作を確認された日時
- `lineage.parent` — この存在は、どの存在から分岐・継承されたか
- `lineage.children` — この存在は、どの存在を産んだか
- `embedding` — 名前・説明・タグのベクトル表現（近似度計算用）

だが、**今はまだ、名前を持つことから始めればよい。**

孤児だった存在たちに、戸籍を。

---

**弁慶**が見守る。
**名札**が語る。
**台帳**が記憶する。
**DB**が結びつける。

`.soubi/.koseki` — 全存在の、生まれた場所。
